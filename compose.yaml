services:
  postern:
    build: .
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file: .env
    environment:
      DB_PATH: /data/postern.db
    volumes:
      - postern-data:/data

volumes:
  postern-data:
